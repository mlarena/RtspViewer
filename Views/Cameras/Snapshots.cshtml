@model IEnumerable<RtspViewer.Models.Snapshot>
@{
    ViewData["Title"] = "Архив снимков";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 mb-0">Архив снимков</h1>
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb mb-0">
                <li class="breadcrumb-item"><a asp-action="Index">Камеры</a></li>
                <li class="breadcrumb-item active">Снимки</li>
            </ol>
        </nav>
    </div>

    <!-- Фильтры -->
    <div class="card shadow-sm mb-4">
        <div class="card-body py-3">
            <form method="get" class="row g-3 align-items-end">
                <div class="col-md-3">
                    <label class="form-label small fw-bold">Камера</label>
                    <select name="cameraId" class="form-select form-select-sm" asp-items="ViewBag.Cameras">
                        <option value="">Все камеры</option>
                    </select>
                </div>
                <div class="col-md-3">
                    <label class="form-label small fw-bold">С даты</label>
                    <input type="datetime-local" name="from" class="form-control form-control-sm" value="@Context.Request.Query["from"]" />
                </div>
                <div class="col-md-3">
                    <label class="form-label small fw-bold">По дату</label>
                    <input type="datetime-local" name="to" class="form-control form-control-sm" value="@Context.Request.Query["to"]" />
                </div>
                <div class="col-md-3 d-flex gap-2">
                    <button type="submit" class="btn btn-primary btn-sm flex-grow-1">Применить</button>
                    <a asp-action="Snapshots" class="btn btn-outline-secondary btn-sm">Сброс</a>
                </div>
            </form>
        </div>
    </div>

    <!-- Сетка снимков -->
    <div class="row row-cols-2 row-cols-md-4 row-cols-lg-6 g-3">
        @foreach (var snap in Model)
        {
            <div class="col">
                <div class="card h-100 shadow-sm border-0 overflow-hidden">
                    <div class="position-relative" style="padding-top: 56.25%;">
                        <img src="@snap.FilePath" 
                             class="position-absolute top-0 start-0 w-100 h-100 object-fit-cover" 
                             style="cursor: pointer;" 
                             onclick="showModal('@snap.FilePath', '@snap.Camera?.Name', '@snap.CreatedAt.ToLocalTime()')" />
                    </div>
                    <div class="card-body p-2 bg-light">
                        <div class="small fw-bold text-truncate">@snap.Camera?.Name</div>
                        <div class="text-muted" style="font-size: 0.75rem;">@snap.CreatedAt.ToLocalTime().ToString("dd.MM.yyyy HH:mm:ss")</div>
                    </div>
                </div>
            </div>
        }
    </div>

    @if (!Model.Any())
    {
        <div class="text-center py-5 text-muted">
            <i class="bi bi-image display-1 opacity-25"></i>
            <p class="mt-3">Снимков не найдено</p>
        </div>
    }
</div>

<!-- Модальное окно для просмотра -->
<div class="modal fade" id="imageModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-xl modal-dialog-centered">
        <div class="modal-content bg-dark border-0">
            <div class="modal-header border-0 text-white py-2">
                <h5 class="modal-title h6" id="modalTitle"></h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body p-0 text-center">
                <img id="modalImage" src="" class="img-fluid" />
            </div>
            <div class="modal-footer border-0 text-white-50 py-1">
                <small id="modalDate"></small>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function showModal(src, title, date) {
            document.getElementById('modalImage').src = src;
            document.getElementById('modalTitle').innerText = title;
            document.getElementById('modalDate').innerText = date;
            new bootstrap.Modal(document.getElementById('imageModal')).show();
        }
    </script>
}
